# Switch 开关

使用开关切换两种互斥的状态。

使用 `import { ISwitch } from "@/components";`

源码 `components/widgets/switch`

## 何时使用

> 开关是在移动设备上调整设置的首选方式。

* 需要表示开关状态/两种互斥的状态的切换时。

* `Checkbox`组件一般用于状态标记，需要和提交操作配合（[Checkboxes vs. Switches](https://uxplanet.org/checkbox-vs-toggle-switch-7fc6e83f10b8)）。

## 代码演示

### 基本用法

<code src='@/examples/switch/basic' />

### 携带文本

<code src='@/examples/switch/copywriter' />

### 异步状态

<code src='@/examples/switch/promise' />

### 自定义UI

<code src='@/examples/switch/custom' />

## API

* 通用属性-`Input`

| 参数 | 类型 | 默认值 | 描述 |
| ---------------- | ------------------- | ------------ | ------------ |
| `checked`        | `boolean`           | `-`          | 控制是否选中。|
| `defaultChecked` | `boolean`           | `-`          | 默认是否选中。|
| `isLoading`      | `boolean`           | `-`          | 控制控件是否加载中。|
| `pattren`        | `ControlPattern`    | `editable`   | 控制控件的交互方式。|
| `size`           | `ControlUISize`     | `medium`     | 控制控件的尺寸大小。|
| `status`         | `ControlUIStatus`   | `success`    | 控制控件的校验状态。|
| `render`         | `ISwitchRenders`    | `-`          | 自定义控件UI。|
